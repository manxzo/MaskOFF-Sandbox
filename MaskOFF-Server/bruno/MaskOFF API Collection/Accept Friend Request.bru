meta {
  name: Accept Friend Request
  type: http
  seq: 19
}

post {
  url: {{base_url}}/friends/accept
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "friendID": "{{friendID}}"
  }
}
