meta {
  name: Verify Email
  type: http
  seq: 2
}

get {
  url: {{base_url}}/verify-email?userID={{userID}}&token={{verificationToken}}
  body: none
  auth: none
}

params:query {
  userID: {{userID}}
  token: {{verificationToken}}
}
